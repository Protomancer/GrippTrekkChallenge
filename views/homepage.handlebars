<!-- Loop over each object in the projects array -->
{{#each hikes as |hike| }}
<div class="row mb-4">
  <div class="col-md-5">
    <h2>
      <a href="/hike/{{hike.id}}">{{hike.hikeName}}</a>
    </h2>
    <div class="col-md-7">
      <p>
        {{hike.time}}
      </p>
      <p> 
        {{hike.hikeLength}}
      </p>
      <p>
        {{hike.elevationChange}}
      </p>
      </div>
      <p>Created by {{hike.user.name}}</p>
  </div>
</div>
{{/each}}